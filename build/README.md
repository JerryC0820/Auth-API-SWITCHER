`icon.ico` and the NSIS installer icons are generated from `public/assets/icons/openai.png` by `npm run build:resources`.

`icon.png` is kept as the current macOS fallback icon source.

Replace the generated assets with your final signed release icons when you have a dedicated `.ico` and `.icns` set.
