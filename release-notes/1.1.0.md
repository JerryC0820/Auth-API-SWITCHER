# 1.1.0

- 云端客服回复修复：控制台发送回复后不再先提示失败、刷新后又显示成功，活动日志异常不会再把已成功写入的消息误判成 `500` 失败。
- 已读状态修复：客服新回复会按消息时间重新判断是否已读，不再因为旧的已读时间残留，导致客户还没打开聊天就被错误标成“已读”。
- 额度刷新提速：悬浮窗、卡片面板和主窗口恢复显示时会主动拉取最新额度，后台刷新频率也缩短，减少长时间停在 `100%` 的滞后问题。
- 在线授权提示收口：客户端在网络抖动、TLS 断开、`ECONNRESET` 或心跳超时时，会统一提示授权服务暂时不可用，不再直接把底层网络错误整段暴露给客户。
