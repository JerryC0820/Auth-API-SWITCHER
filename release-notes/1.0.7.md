# 1.0.7

- 在线更新热修：Windows 端下载完成后会先直接拉起安装器，再由安装器提示关闭正在运行的软件，修复旧版本先退出但安装器没有弹出的链路问题。
- 安装失败可重试：如果安装器启动失败，当前程序不会黑屏退出，下载好的安装包会继续保留，本地可以直接再次点击“开始安装更新”重试。
- Team 命名修正：团队空间现在会优先从 OpenAI 后端解析真实空间名称；如果当前账号拿不到真实团队名，也会按 `Team`、`Team2` 这类规则回退，不再误显示成 `Personal`。
