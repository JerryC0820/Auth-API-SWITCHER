# 0.1.8 Test

- 修复 Windows 更新下载完成后的安装接力问题；原先 `0.1.6 -> 0.1.7` 仍可能出现“应用关掉了但安装器没有拉起”的情况，这一版把接力逻辑改为更稳的 PowerShell 启动链，并补上强制退出兜底。
- 接力进程现在最多等待旧进程 45 秒；如果旧进程没有完全退出，也会继续尝试拉起安装器，避免因为托盘残留或退出链路卡住而一直不安装。
- 如果安装器启动失败，接力进程会自动回退到打开安装包所在位置，避免再次出现静默无反馈。
