# 1.0.8

- 启动提速：启动后会优先把空间卡片显示出来，额度与探活改为后台补跑，不再让首屏长时间卡在大图标转圈。
- OAuth 自动识别：Codex / ChatGPT 登录成功后，当前 `auth.json` 会自动识别并入卡，不再需要手动刷新、重启或再点一次导入。
- 导入动画收口：新空间导入会先立即出卡，再在后台同步额度与状态，避免长时间停在翻牌和转圈阶段。
- 头像显示统一：顶部头像、用户中心和上传预览统一按圆形效果显示，减少头像被额外裁切的问题。
- 更新下载兜底：强更弹窗补齐官网、GitHub、Gitee 手动下载出口，下载校验失败时会继续尝试镜像源。
