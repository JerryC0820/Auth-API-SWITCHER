# 1.0.9 Test

- 内测重点一：确认 `https://admin.ainivox.cn/root-console/` 登录后右上角不再显示“本地模式”，并能直接看到客户与授权列表。
- 内测重点二：确认浏览器版后台可以正常拉取运营总览、客户分组、订单发货与客服反馈，不再出现 `Failed to fetch` 或 `404`。
- 内测重点三：确认本地 `127.0.0.1` 浏览器端口可以直接登录云端控制台，用于发版前回归验证。
