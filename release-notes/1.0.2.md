# 1.0.2

- 重启链路修复：修正 npm 开发版和安装版的应用重启判定与接力方式，避免点击“重启应用”后直接退出但没有重新拉起。
- 安装完成启动修复：安装器完成页的“立即启动”改为更稳的可执行文件直启逻辑，减少安装完成后勾选启动却没有真正打开的问题。
- 在线更新安装修复：Windows 在线更新改成先下载并校验，再由界面明确进入“开始安装更新”步骤，不再在下载完成后直接关闭应用却没有弹出覆盖安装器。
- 更新交接稳定性补强：Windows 的重启和安装器接力统一改为更稳的独立命令启动方式，降低应用退出时把交接进程一并带掉的概率。
