# 2.0.0 Test

- 内测入口回到 `2.0.0`：`latest_test.json` 继续保留内测通道结构，但版本号、说明链接和下载资产统一指向 `v2.0.0`。
- 更新链路修复：安装版当前版本统一显示为 `2.0.0`，Windows 在线更新会自动退出旧进程再启动安装器。
- 认证入口重做：扫码页拆出独立布局，手动登录补回“记住密码”，关闭按钮统一为返回按钮风格。
- 云端覆盖与删除提速：批量删除、全部删除和云端覆盖本地都改成真正批量路径，不再逐张慢删。
- 统计与扫码绑定修复：统计中心与空间池状态口径统一，扫码绑定支持更多上游身份字段结构。
