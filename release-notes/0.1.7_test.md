# 0.1.7 Test

- 修复强制更新卡片的点击穿透问题；更新按钮现在应只响应当前前景卡片，不会再点到后面的更新摘要。
- 调整 Windows 更新安装接力流程；下载完成后会在当前应用退出后继续拉起安装器，避免“软件关掉了但没有进入安装”的情况。
- 打包安装器与安装完成后的程序图标改为新的程序图标资源；运行中的任务栏与托盘图标继续使用透明无边框的 OpenAI 图标。
- 用户中心里的悬浮窗开关与透明度调节改为本地即时响应，拖动和切换不再需要等完整设置回写后才反馈。
- 卡片张数现在按授权上限收口；例如授权 4 张时，第 5 张开始会直接拒绝添加，主界面可见张数也会按上限显示。
