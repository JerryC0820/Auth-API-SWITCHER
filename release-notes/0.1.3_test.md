# 0.1.3 Test

- 本轮为内测通道预发布包，优先验证 beta 覆盖文件、内测授权码自动切通道、以及 `latest_test.json` 更新链路。
- 本地授权主机新增按机器指纹和授权约束复用旧授权码的能力，方便长期管理测试设备。
- 主界面、关于页、更新弹窗会显示内测用户标识和测试标识，便于区分测试机与正式机。
- 修复授权主机窗口标题栏随内容滚动的问题。
- 如果需要把某台机器临时切到内测通道，可复制 `update-channel.override.json` 到客户端约定位置后重启。
