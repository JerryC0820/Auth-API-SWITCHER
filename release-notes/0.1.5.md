# 0.1.5

- 强制更新拦截层现在会覆盖整个应用工作区；一旦检测到必须更新，底层切换、额度预警和推荐切换交互会被统一暂停，不再允许继续使用核心功能。
- 强制更新状态下会主动关闭额度预警 / 自动切换提示，并拦截新的切换动作，避免旧版本在未更新完成前继续执行空间切换。
- 强制更新卡片补上“重启应用”和“关闭应用”按钮；当下载异常或需要重新拉起时，用户可以直接在拦截层处理，不必再绕到别的入口。
