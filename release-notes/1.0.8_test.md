# 1.0.8 Test

- Team 停用补扫：启动或刷新时会优先补扫历史上因为 `Unexpected end of JSON input` 漏判的 Team / Business 卡，并在命中 `deactivated_workspace` 后立即写回“已停用”状态。
- OAuth 即时刷新：OAuth 登录成功后，如果命中已有空间卡片，会立刻重探活并落盘，不再只停留在内存里或等到重启后才更新。
- 后台登录态收口：`root-console` 管理后台不再把管理员 token 长期写入浏览器本地存储，关闭浏览器后需要重新登录，旧的持久化 token 也会自动清理。
- 内测重点一：确认 `1657958850@qq.com` 与 `3132733035@qq.com` 两张 Team 卡会自动切到“已停用”，而 `MyTeam` 与 `AI Team Workspace` 继续保持正常。
- 内测重点二：确认 `https://admin.ainivox.cn/root-console/` 在关闭浏览器后不会再直接免密进入后台。
